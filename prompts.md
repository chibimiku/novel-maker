我需要一个python写的基于大语言模型的小说创作器。
创作器自身包含conf目录下的一个json文件，setting.json，包含大语言模型的endpoint地址，key，模型，instructions等设置，大语言模型支持配置两个类型，产生文本和根据需求画图两个。endpoint类型可设置符合openai或gemini等风格。
小说创作应该具备可以打开或者创建一个工程目录，里面包括了公共设定、人物设定、名词设定、地点设定、其他设定5个设定目录，1个正文目录，1个系统数据目录。小说正文应该分为章、节、场景三个层级组成这部小说。
系统数据目录里包含了小说的总体概要目录，以json树状结构记录了小说正文目录的每个部分（章、节、场景）的概要和对应文件的md5hash，用于生成新的内容时传入的上下文。
人物设定等5个设定目录，各提供一个template.json用于给用户提供指引，例如人物设定的template.json里有姓名、年龄、性别等字段，提示用户新建一个相应设定时，应该输入哪些基础字段。当然，也支持用户自由拓展自定义字段。
正文目录则分层级保存章、节、场景的概要和正文（只有场景同时包括概要和正文，章和节只包括概要。），以markdown的格式。如果文章认为这里应该有插图，则以一种约定的格式留下描述符，引用当前目录 /images 下面的图片（这个预设点支持后续用户点击后单独请求画图模型来生成）。

接下来，我描述用户界面和功能：
屏幕上方提供一个菜单，包括文件、设置、关于，其中文件支持打开工作区和保存全部，设置则提供一个单独界面配置系统数据，注意配置endpoint后，还提供一个“获取”按钮拉取当前服务商支持的具体模型类型。
下方是工作区，从左往右数：
（1）最左边树状结构从上到下显示各种设定目录，可以展开并手动选择每一个类型的设定，打钩的设定将在生成文件时参与上下文，反正则不。每个类型的设定最下方都有一个加号，点击后根据template.json弹出输入框，用户根据输入
（2）第二个是小说的章、节、场景三级树状目录结构，同样的每个层级的目录下方都有一个加号，支持输入新增这部分概要后，自动结合已勾选的各种设定，加上目录上下文，以及小说目录上层节点的概要（例如生成某一场景的内容则会引入对应章、节的概要，以及上一场景和下一场景的概要以确保内容连贯），拼接成一个prompts请求llm，拿到结果后拼接成正文，生成新的Markdown文本并保存，根据新文件更新目录json。
在载入工作区时，允许有对应文件不存在的情况，将对应章、节、场景显示为灰色。用户可勾选多个节点，根据概要依次生成内容，当然生成后需要更新目录json。
（3）最右边是详情界面，占据比较大的空间，会显示当前选中节点的内容，包括概要和正文，正文markdown的形式展示，用户可手动编辑文本并保存，也可点击重新生成来生成再次生成这个节点的正文。
工作区下方有一条显示log的地方，同时log也会打印在这个python工具的/log目录下
